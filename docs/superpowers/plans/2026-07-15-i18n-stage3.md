# i18n Stage 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish the TUI's user-visible English: decision-modal option labels, the six untranslated popups, and the ~145-site statusMsg tail — plus the carried polish items and a QA delta pass.

**Architecture:** Unchanged (spec: `docs/superpowers/specs/2026-07-15-i18n-stage3-design.md`; stages 1–2 specs alongside). English-text-as-key `i18n.T`; the `internal/tui/i18n_display.go` render seam grows `optionDisplayName`; a new AST scan test forces every decision-option value through the bundle chain.

**Tech Stack:** Go 1.26, Bubble Tea / lipgloss, go/ast + go/parser (scan tests), BurntSushi TOML.

## Global Constraints

Every task's requirements implicitly include all of these:

- **Worktree:** all work happens in `/mnt/t/others/gigagit/.claude/worktrees/i18n-stage3` on branch `feat/i18n-stage3`. `cd` there first; verify with `git branch --show-current`. Use absolute paths under the worktree for every file edit.
- **Bundle discipline:** any NEW `i18n.T` key gains an entry in ALL FOUR bundles (`internal/i18n/lang/{ja,ko,zh,ru}.toml`) in the same commit; any key no longer referenced is DELETED from all four (orphan check). Bundle key order is NOT enforced — insert near related keys, never re-sort. Grep before adding: an existing key is reused, never duplicated (duplicate TOML key = load error).
- **Literal keys only:** `i18n.T`'s first argument is a string literal (scan-test enforced).
- **Protocol strings stay English:** decision option VALUES (`Options:` lists, `onResolve`/decider comparisons, the esc→`"abort"` mapping), config values, op/source identity, notice/action IDs. Translation happens ONLY at render.
- **Full-sentence keys:** no fragment concatenation (footer hint strips remain the sanctioned exception). Error text stays English, passed as a `%s` arg. Counts follow the two-key singular/plural convention where English inflects.
- **Display math** on translated strings uses `lipgloss.Width`/`padCell`, never `len()`/`%-Ns`.
- **Translation authoring** (extraction tasks): keep `[key]` tokens, glyphs, `%s`/`%d` verbs exact; match existing bundle tone (grep a few neighboring entries first); the verb checker and scan test are the gates.
- **Per-task verification:** `go test ./internal/i18n/ ./internal/tui/ -count=1` passes before each commit.
- **Commit convention:** one commit per task (unless the task says otherwise), message given in the task, ending with:

  ```
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01VenKpykpf4qnUo9xgnqbiE
  ```

---

### Task 1: `optionDisplayName` + modal render wiring + vocabulary

**Files:**
- Modify: `internal/tui/i18n_display.go` (append func)
- Modify: `internal/tui/i18n_display_test.go` (append test)
- Modify: `internal/tui/view.go` (`renderModal`, ~lines 1551–1575: the option loop + footer)
- Modify: all four bundles (~40 new keys; translations given below VERBATIM)

**Interfaces:**
- Consumes: nothing new.
- Produces: `func optionDisplayName(value string) string` — Task 2's scan test enforces its coverage; no other task calls it.

Background: ALL decision modals (engine `DecisionNeeded` forks and TUI `decisionState` confirms) render through one loop in `renderModal`. Dispatch is by cursor index; `resolveModal(m.modal.req.Options[m.modal.sel])` and the esc→`abortOption` mapping always operate on the raw VALUE — so translating the rendered label is safe with zero dispatch changes.

- [ ] **Step 1: Write the failing test** (append to `i18n_display_test.go`):

```go
func TestOptionDisplayNameEnglishPassthrough(t *testing.T) {
	// English is the key: with no language set, every value maps to itself.
	for _, v := range []string{
		"Apply patch", "Cancel", "Cherry-pick", "Create branch…",
		"Create worktree…", "Delete", "Detached", "Discard", "No",
		"Push branch + tags", "Push branch only", "Remove",
		"Reorder & squash", "Yes", "abort", "cancel",
		"check out as different name…", "checkout-and-resolve", "commits",
		"delete", "force", "force-delete", "force-with-lease",
		"go to worktree", "hard", "keep", "keep-conflicts", "merge",
		"mixed", "no", "proceed", "rebase", "reset", "run", "skip", "soft",
		"unlock-and-remove", "working-tree", "yes",
	} {
		if got := optionDisplayName(v); got != v {
			t.Fatalf("optionDisplayName(%q) = %q, want passthrough", v, got)
		}
	}
	if got := optionDisplayName("feature/dynamic-branch-name"); got != "feature/dynamic-branch-name" {
		t.Fatalf("unknown value must pass through, got %q", got)
	}
}
```

- [ ] **Step 2: Run to verify it fails** — `go test ./internal/tui/ -run TestOptionDisplayName -count=1` — Expected: FAIL (undefined: optionDisplayName).

- [ ] **Step 3: Implement** (append to `i18n_display.go`):

```go
// optionDisplayName translates a decision-option label for display in the
// modal. Option VALUES are protocol — Options lists, onResolve/decider
// comparisons, and the esc→"abort" mapping keep the English word; ONLY
// renderModal's option loop comes here. A value with no case (e.g. a
// dynamic name) passes through — names must not be translated anyway.
// Task: internal/tui/options_vocab_test.go forces every statically declared
// option value through the bundles; the bundle orphan check forces each
// bundle key back to a T() literal, which is the case arm here.
func optionDisplayName(value string) string {
	switch value {
	case "Apply patch":
		return i18n.T("Apply patch")
	case "Cancel":
		return i18n.T("Cancel")
	case "Cherry-pick":
		return i18n.T("Cherry-pick")
	case "Create branch…":
		return i18n.T("Create branch…")
	case "Create worktree…":
		return i18n.T("Create worktree…")
	case "Delete":
		return i18n.T("Delete")
	case "Detached":
		return i18n.T("Detached")
	case "Discard":
		return i18n.T("Discard")
	case "No":
		return i18n.T("No")
	case "Push branch + tags":
		return i18n.T("Push branch + tags")
	case "Push branch only":
		return i18n.T("Push branch only")
	case "Remove":
		return i18n.T("Remove")
	case "Reorder & squash":
		return i18n.T("Reorder & squash")
	case "Yes":
		return i18n.T("Yes")
	case "abort":
		return i18n.T("abort")
	case "cancel":
		return i18n.T("cancel")
	case "check out as different name…":
		return i18n.T("check out as different name…")
	case "checkout-and-resolve":
		return i18n.T("checkout-and-resolve")
	case "commits":
		return i18n.T("commits")
	case "delete":
		return i18n.T("delete")
	case "force":
		return i18n.T("force")
	case "force-delete":
		return i18n.T("force-delete")
	case "force-with-lease":
		return i18n.T("force-with-lease")
	case "go to worktree":
		return i18n.T("go to worktree")
	case "hard":
		return i18n.T("hard")
	case "keep":
		return i18n.T("keep")
	case "keep-conflicts":
		return i18n.T("keep-conflicts")
	case "merge":
		return i18n.T("merge")
	case "mixed":
		return i18n.T("mixed")
	case "no":
		return i18n.T("no")
	case "proceed":
		return i18n.T("proceed")
	case "rebase":
		return i18n.T("rebase")
	case "reset":
		return i18n.T("reset")
	case "run":
		return i18n.T("run")
	case "skip":
		return i18n.T("skip")
	case "soft":
		return i18n.T("soft")
	case "unlock-and-remove":
		return i18n.T("unlock-and-remove")
	case "working-tree":
		return i18n.T("working-tree")
	case "yes":
		return i18n.T("yes")
	}
	return value
}
```

- [ ] **Step 4: Wire `renderModal`** (`view.go` ~1551): in the option loop change

```go
		wrapped := wrapWords(opt, optW)
```
to
```go
		wrapped := wrapWords(optionDisplayName(opt), optW)
```

and the footer literal below it:

```go
	footer := i18n.T("[↑/↓] choose  [enter] confirm  [esc] abort")
```

Nothing else in `renderModal` changes (`m.modal.req.Prompt` is already translated at construction for TUI modals; engine prompts stay English by design).

- [ ] **Step 5: Scan test → bundle entries.** `"merge"`, `"rebase"`, `"cancel"`, `"commits"` already exist (reuse — do NOT re-add). `"cherry-pick"`/`"revert"` lowercase exist but are NOT in this vocabulary (the modal uses Title-case `"Cherry-pick"`). Add these to ALL FOUR bundles (one block per language, verbatim; `soft`/`mixed`/`hard` deliberately stay English — git reset modes are technical vocabulary, the `"reflog" = "reflog"` precedent):

ja.toml:
```toml
"Apply patch" = "パッチを適用"
"Cancel" = "キャンセル"
"Cherry-pick" = "チェリーピック"
"Create branch…" = "ブランチを作成…"
"Create worktree…" = "ワークツリーを作成…"
"Delete" = "削除"
"Detached" = "デタッチ (ブランチなし)"
"Discard" = "破棄"
"No" = "いいえ"
"Push branch + tags" = "ブランチ+タグをプッシュ"
"Push branch only" = "ブランチのみプッシュ"
"Remove" = "除去"
"Reorder & squash" = "並べ替えてスカッシュ"
"Yes" = "はい"
"[↑/↓] choose  [enter] confirm  [esc] abort" = "[↑/↓] 選択  [enter] 確定  [esc] 中止"
"abort" = "中止"
"check out as different name…" = "別名でチェックアウト…"
"checkout-and-resolve" = "チェックアウトして解決"
"delete" = "削除"
"force" = "強制"
"force-delete" = "強制削除"
"force-with-lease" = "強制 (with-lease)"
"go to worktree" = "ワークツリーへ移動"
"hard" = "hard"
"keep" = "保持"
"keep-conflicts" = "競合を残す"
"mixed" = "mixed"
"no" = "いいえ"
"proceed" = "続行"
"reset" = "リセット"
"run" = "実行"
"skip" = "スキップ"
"soft" = "soft"
"unlock-and-remove" = "ロック解除して除去"
"working-tree" = "作業ツリーの変更"
"yes" = "はい"
```

ko.toml:
```toml
"Apply patch" = "패치 적용"
"Cancel" = "취소"
"Cherry-pick" = "체리픽"
"Create branch…" = "브랜치 생성…"
"Create worktree…" = "워크트리 생성…"
"Delete" = "삭제"
"Detached" = "분리됨 (detached)"
"Discard" = "버리기"
"No" = "아니요"
"Push branch + tags" = "브랜치+태그 푸시"
"Push branch only" = "브랜치만 푸시"
"Remove" = "제거"
"Reorder & squash" = "재정렬 후 스쿼시"
"Yes" = "예"
"[↑/↓] choose  [enter] confirm  [esc] abort" = "[↑/↓] 선택  [enter] 확정  [esc] 중단"
"abort" = "중단"
"check out as different name…" = "다른 이름으로 체크아웃…"
"checkout-and-resolve" = "체크아웃 후 해결"
"delete" = "삭제"
"force" = "강제"
"force-delete" = "강제 삭제"
"force-with-lease" = "강제 (with-lease)"
"go to worktree" = "워크트리로 이동"
"hard" = "hard"
"keep" = "유지"
"keep-conflicts" = "충돌 유지"
"mixed" = "mixed"
"no" = "아니요"
"proceed" = "진행"
"reset" = "리셋"
"run" = "실행"
"skip" = "건너뛰기"
"soft" = "soft"
"unlock-and-remove" = "잠금 해제 후 제거"
"working-tree" = "작업 트리 변경"
"yes" = "예"
```

ru.toml:
```toml
"Apply patch" = "Применить патч"
"Cancel" = "Отмена"
"Cherry-pick" = "Черри-пик"
"Create branch…" = "Создать ветку…"
"Create worktree…" = "Создать рабочее дерево…"
"Delete" = "Удалить"
"Detached" = "Detached (без ветки)"
"Discard" = "Отбросить"
"No" = "Нет"
"Push branch + tags" = "Отправить ветку + теги"
"Push branch only" = "Отправить только ветку"
"Remove" = "Убрать"
"Reorder & squash" = "Переупорядочить и объединить"
"Yes" = "Да"
"[↑/↓] choose  [enter] confirm  [esc] abort" = "[↑/↓] выбор  [enter] подтвердить  [esc] прервать"
"abort" = "прервать"
"check out as different name…" = "переключиться под другим именем…"
"checkout-and-resolve" = "переключиться и разрешить"
"delete" = "удалить"
"force" = "принудительно"
"force-delete" = "удалить принудительно"
"force-with-lease" = "принудительно (with-lease)"
"go to worktree" = "перейти в рабочее дерево"
"hard" = "hard"
"keep" = "оставить"
"keep-conflicts" = "оставить конфликты"
"mixed" = "mixed"
"no" = "нет"
"proceed" = "продолжить"
"reset" = "сбросить"
"run" = "выполнить"
"skip" = "пропустить"
"soft" = "soft"
"unlock-and-remove" = "разблокировать и убрать"
"working-tree" = "в рабочее дерево"
"yes" = "да"
```

zh.toml:
```toml
"Apply patch" = "应用补丁"
"Cancel" = "取消"
"Cherry-pick" = "拣选"
"Create branch…" = "创建分支…"
"Create worktree…" = "创建工作树…"
"Delete" = "删除"
"Detached" = "分离 (detached)"
"Discard" = "丢弃"
"No" = "否"
"Push branch + tags" = "推送分支+标签"
"Push branch only" = "仅推送分支"
"Remove" = "移除"
"Reorder & squash" = "重排并压缩"
"Yes" = "是"
"[↑/↓] choose  [enter] confirm  [esc] abort" = "[↑/↓] 选择  [enter] 确定  [esc] 中止"
"abort" = "中止"
"check out as different name…" = "以其他名称检出…"
"checkout-and-resolve" = "检出并解决"
"delete" = "删除"
"force" = "强制"
"force-delete" = "强制删除"
"force-with-lease" = "强制 (with-lease)"
"go to worktree" = "前往工作树"
"hard" = "hard"
"keep" = "保留"
"keep-conflicts" = "保留冲突"
"mixed" = "mixed"
"no" = "否"
"proceed" = "继续"
"reset" = "重置"
"run" = "运行"
"skip" = "跳过"
"soft" = "soft"
"unlock-and-remove" = "解锁并移除"
"working-tree" = "工作树更改"
"yes" = "是"
```

- [ ] **Step 6: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add internal/tui/i18n_display.go internal/tui/i18n_display_test.go internal/tui/view.go internal/i18n/lang/
git commit -m "feat(tui): translate decision-modal option labels at the single render site"
```

---

### Task 2: Option-vocabulary AST scan test

**Files:**
- Create: `internal/tui/options_vocab_test.go`

**Interfaces:**
- Consumes: `i18n.Builtins() map[string]map[string]string` (existing); Task 1's `optionDisplayName` (its body is scanned).
- Produces: the enforcement gate — nothing calls it.

The chain this closes: a statically declared option value must be a bundle key (this test) → a bundle key must be a live `T()` literal (existing orphan check in `TestI18nBundlesComplete`) → the natural home for that literal is an `optionDisplayName` case. Part (b) of this test additionally verifies the case exists, so a translated-but-not-rendered value can't slip through.

- [ ] **Step 1: Write the test** (complete file):

```go
package tui

import (
	"go/ast"
	"go/parser"
	"go/token"
	"os"
	"strconv"
	"strings"
	"testing"

	"github.com/homeend/gigagit/internal/i18n"
)

// collectOptionValues walks a package directory and returns every element of
// every `Options: []string{…}` composite literal in non-test sources. String
// literals are taken directly; identifiers are resolved through the package's
// own top-level string constants (e.g. engine's applyOptWorkingTree). An
// element it cannot resolve statically is reported — dynamic option values
// belong in a variable-built slice, not a literal.
func collectOptionValues(t *testing.T, dir string) map[string]string {
	t.Helper()
	fset := token.NewFileSet()
	pkgs, err := parser.ParseDir(fset, dir, func(fi os.FileInfo) bool {
		return !strings.HasSuffix(fi.Name(), "_test.go")
	}, 0)
	if err != nil {
		t.Fatalf("parse %s: %v", dir, err)
	}
	out := map[string]string{}
	for _, pkg := range pkgs {
		consts := map[string]string{}
		for _, f := range pkg.Files {
			for _, decl := range f.Decls {
				gd, ok := decl.(*ast.GenDecl)
				if !ok || gd.Tok != token.CONST {
					continue
				}
				for _, spec := range gd.Specs {
					vs, ok := spec.(*ast.ValueSpec)
					if !ok {
						continue
					}
					for i, name := range vs.Names {
						if i < len(vs.Values) {
							if bl, ok := vs.Values[i].(*ast.BasicLit); ok && bl.Kind == token.STRING {
								if v, err := strconv.Unquote(bl.Value); err == nil {
									consts[name.Name] = v
								}
							}
						}
					}
				}
			}
		}
		for _, f := range pkg.Files {
			ast.Inspect(f, func(n ast.Node) bool {
				kv, ok := n.(*ast.KeyValueExpr)
				if !ok {
					return true
				}
				key, ok := kv.Key.(*ast.Ident)
				if !ok || key.Name != "Options" {
					return true
				}
				cl, ok := kv.Value.(*ast.CompositeLit)
				if !ok {
					// A dynamic slice is legitimate (values are names; passthrough
					// renders them correctly) — but keep it visible, never silent.
					t.Logf("%s: Options is not a composite literal — dynamic values render untranslated by design", fset.Position(kv.Pos()))
					return true
				}
				for _, el := range cl.Elts {
					pos := fset.Position(el.Pos()).String()
					switch e := el.(type) {
					case *ast.BasicLit:
						if e.Kind == token.STRING {
							if v, err := strconv.Unquote(e.Value); err == nil {
								out[v] = pos
							}
						}
					case *ast.Ident:
						if v, ok := consts[e.Name]; ok {
							out[v] = pos
						} else {
							t.Errorf("%s: option element %s is not a same-package string const — use a literal or add resolution", pos, e.Name)
						}
					default:
						t.Errorf("%s: option element is neither a string literal nor a const identifier", pos)
					}
				}
				return true
			})
		}
	}
	return out
}

// optionDisplayCaseKeys returns the set of i18n.T literal keys inside
// optionDisplayName's body — its case arms.
func optionDisplayCaseKeys(t *testing.T) map[string]bool {
	t.Helper()
	fset := token.NewFileSet()
	f, err := parser.ParseFile(fset, "i18n_display.go", nil, 0)
	if err != nil {
		t.Fatalf("parse i18n_display.go: %v", err)
	}
	keys := map[string]bool{}
	for _, decl := range f.Decls {
		fd, ok := decl.(*ast.FuncDecl)
		if !ok || fd.Name.Name != "optionDisplayName" {
			continue
		}
		ast.Inspect(fd.Body, func(n ast.Node) bool {
			call, ok := n.(*ast.CallExpr)
			if !ok || len(call.Args) == 0 {
				return true
			}
			sel, ok := call.Fun.(*ast.SelectorExpr)
			if !ok || sel.Sel.Name != "T" {
				return true
			}
			if bl, ok := call.Args[0].(*ast.BasicLit); ok && bl.Kind == token.STRING {
				if v, err := strconv.Unquote(bl.Value); err == nil {
					keys[v] = true
				}
			}
			return true
		})
	}
	if len(keys) == 0 {
		t.Fatal("optionDisplayName not found or has no T() cases")
	}
	return keys
}

// TestDecisionOptionValuesTranslated: every statically declared decision
// option value across engine+tui must (a) exist as a key in all four
// bundles and (b) have an optionDisplayName case, so the modal renders it
// translated. Dynamic values (names) legitimately pass through.
func TestDecisionOptionValuesTranslated(t *testing.T) {
	values := map[string]string{}
	for _, dir := range []string{".", "../engine"} {
		for v, pos := range collectOptionValues(t, dir) {
			values[v] = pos
		}
	}
	if len(values) < 30 {
		t.Fatalf("collected only %d option values — the scan is likely broken", len(values))
	}
	cases := optionDisplayCaseKeys(t)
	builtins := i18n.Builtins()
	for v, pos := range values {
		if !cases[v] {
			t.Errorf("option %q (%s): no optionDisplayName case — it would render untranslated", v, pos)
		}
		for _, code := range []string{"ja", "ko", "zh", "ru"} {
			if _, ok := builtins[code][v]; !ok {
				t.Errorf("option %q (%s): missing from %s.toml", v, pos, code)
			}
		}
	}
}
```

- [ ] **Step 2: Run** `go test ./internal/tui/ -run TestDecisionOptionValues -count=1` — Expected: PASS (Task 1 landed the vocabulary). If it FAILS, the failure list is real missed vocabulary: add the missing case + 4 bundle entries (translate in the Task 1 style) rather than weakening the test.

- [ ] **Step 3: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/tui/options_vocab_test.go internal/tui/i18n_display.go internal/i18n/lang/
git commit -m "test(tui): AST scan forces every decision-option value through the translation chain"
```

---

### Task 3: `padCell` relocation + identity popup

**Files:**
- Modify: `internal/tui/settings_popup.go` (remove `padCell`, ~line 617)
- Modify: `internal/tui/i18n_display.go` (add `padCell` — verbatim move)
- Modify: `internal/tui/identity_popup.go` (extraction + pads at lines 348, 400, 429)
- Test: `internal/tui/i18n_display_test.go` (padCell alignment test), plus scan test as gate
- Modify: all four bundles (implementer authors translations)

**Interfaces:**
- Consumes: nothing new. Produces: `padCell(s string, w int) string` now lives in `i18n_display.go` (same signature; callers in settings_popup.go unchanged).

- [ ] **Step 1: Move `padCell`** verbatim from `settings_popup.go:617` into `i18n_display.go` (keep its doc comment; it now has two call-site files). Run `go build ./internal/tui/` — must compile.
- [ ] **Step 2: Failing alignment test** (append to `i18n_display_test.go`):

```go
func TestIdentityLabelPadCellAlignsCJK(t *testing.T) {
	ascii := "  " + padCell("Name", 9) + " v"
	cjk := "  " + padCell("名前", 9) + " v"
	if lipgloss.Width(ascii) != lipgloss.Width(cjk) {
		t.Fatalf("padded label columns misalign: %d vs %d", lipgloss.Width(ascii), lipgloss.Width(cjk))
	}
}
```

(Add the `lipgloss` import to the test file if missing.) This passes as soon as padCell is available — it pins the property the identity rows rely on.

- [ ] **Step 3: Fix the three pads in `identity_popup.go`:**

Line 348: `return fmt.Sprintf("  %-9s %s", label, val)` → `return "  " + padCell(label, 9) + " " + val`
Line 400: `return viewField(fmt.Sprintf("%s%-10s ", cursor, label), f, focused, contentWidth)` → `return viewField(cursor+padCell(label, 10)+" ", f, focused, contentWidth)`
Line 429: `fmt.Sprintf("%s%-10s %s", scopeCursor, "Scope", scopeVal)` → `scopeCursor + padCell(i18n.T("Scope"), 10) + " " + scopeVal`

- [ ] **Step 4: Extract every user-facing literal in `identity_popup.go`** (~440 lines): read the whole file's render/update funcs; wrap titles, field labels (Name/Email/Scope/profile-name), list headers, hints/footers, and its 2 statusMsgs (`"name and email are required"`, `"profile name, git name and email are required"`). Protocol rules: `model.ProfileScope` VALUES and config scope strings (`"local"`/`"global"`) stay English in state/comparisons — if a scope renders raw, route it through a small literal-key switch in this file (the `agentStatusDisplay` pattern in settings_popup.go). Do not touch git config keys (`user.name`, `user.email`) — identity.
- [ ] **Step 5: Scan test → author translations ×4** for every new key (match bundle tone).
- [ ] **Step 6: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS (existing identity tests assert English: unchanged).
- [ ] **Step 7: Commit**

```bash
git add internal/tui/settings_popup.go internal/tui/i18n_display.go internal/tui/i18n_display_test.go internal/tui/identity_popup.go internal/i18n/lang/
git commit -m "feat(tui): translate the identity & profiles popup; padCell shared for CJK-safe label columns"
```

---

### Task 4: Git-config explorer popup

**Files:**
- Modify: `internal/tui/gitconfig_popup.go` (664 lines)
- Modify: all four bundles (implementer authors translations)

**Interfaces:** consumes nothing new; produces nothing.

- [ ] **Step 1: Extract every user-facing literal**: the window title, column headers (key/local/global/default), the dim `(unset)` placeholder, edit-mode prompts (option picker / textfield labels / unset chooser), `z`-mode labels, footers/hints, its statusMsg (`"nothing to unset"`), and load/error notices. Protocol rules: git config KEY names, VALUES being written (`"true"`, enum values from `gitconfdocs`), and scope words used in `git config` argv stay English; a scope word that only renders gets a literal-key display switch. `gitconfdocs` catalog descriptions are package data — OUT of scope (do not translate; they come from `internal/gitconfdocs`, an English catalog).
- [ ] **Step 2: Watch display math**: the popup lays columns — if any width math uses `len()` on a now-translated string, switch it to `lipgloss.Width`/`padCell` (grep `len(` in the file and judge each).
- [ ] **Step 3: Scan test → translations ×4.**
- [ ] **Step 4: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS.
- [ ] **Step 5: Commit**

```bash
git add internal/tui/gitconfig_popup.go internal/i18n/lang/
git commit -m "feat(tui): translate the git-config explorer popup"
```

---

### Task 5: Notice + repo-config popups

**Files:**
- Modify: `internal/tui/notice_popup.go` (192 lines), `internal/tui/notify.go` (notice text builders, if the literals live there — grep), `internal/tui/repoconfig_popup.go` (255 lines)
- Modify: all four bundles (implementer authors translations)

**Interfaces:** consumes nothing new; produces nothing.

- [ ] **Step 1: Notice popup**: wrap the dialog title, action-row labels ("Not now", "Never for this repo", the write+enable action label — reuse the existing `"Not now"` key from stage 2), footers, and the notice body texts where they are TUI-authored literals (grep `notice` text construction across `notify.go`/`notice_popup.go`). Protocol: notice IDs (`clipboard_tool_missing`, etc.) and `promptstate` keys stay English. CAUTION: check how action rows dispatch — if any dispatch compares label text (rather than an index/id), route display through a lookup and keep the compared value English; report what you found either way.
- [ ] **Step 2: Repo-config popup**: wrap the location rows (committed vs private descriptions), confirmation prompts, and its 3 statusMsgs. Protocol: file paths and the location identity values stay as args.
- [ ] **Step 3: Scan test → translations ×4.**
- [ ] **Step 4: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS.
- [ ] **Step 5: Commit**

```bash
git add internal/tui/notice_popup.go internal/tui/notify.go internal/tui/repoconfig_popup.go internal/i18n/lang/
git commit -m "feat(tui): translate the notification-center and repo-config-location popups"
```

---

### Task 6: Review viewer + hook editor

**Files:**
- Modify: `internal/tui/review_view.go` (224 lines), `internal/tui/hook_editor.go` (106 lines)
- Modify: all four bundles (implementer authors translations)

**Interfaces:** consumes nothing new; produces nothing.

- [ ] **Step 1: Review viewer**: chrome only — title prefix, search prompt/`(no match)` hints, footer (`[e]` editor / `[/]` search / `[esc]` close hints). The report CONTENT is agent output — NEVER translated.
- [ ] **Step 2: Hook editor**: title, editing hints (Enter=newline / Ctrl+S=save / Esc=cancel footer), and its 3 statusMsgs (`"hook set (not saved: no repo config path)"`, `"hook set but not saved: %s"` shape, `"post-create hook saved"`). Error text stays an arg.
- [ ] **Step 3: Scan test → translations ×4.**
- [ ] **Step 4: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS.
- [ ] **Step 5: Commit**

```bash
git add internal/tui/review_view.go internal/tui/hook_editor.go internal/i18n/lang/
git commit -m "feat(tui): translate the review viewer chrome and hook editor"
```

---

### Task 7: statusMsg wave 1 — model.go

**Files:**
- Modify: `internal/tui/model.go` (57 sites)
- Modify: all four bundles (implementer authors translations)

**Interfaces:** consumes existing display funcs (`opDisplayName`, `sourceDisplayName` — use them when a protocol value renders inside a message); produces nothing.

- [ ] **Step 1: Enumerate**: `grep -n 'statusMsg = "\|statusMsg += "' internal/tui/model.go` — every hit is in scope unless it writes an already-translated variable.
- [ ] **Step 2: Wrap each site** by the standing rules: error text → `%s` arg; concatenated middles → full-sentence key with args; counts → two-key plural where English inflects (grep the surrounding code for the count variable); protocol values interpolate via display funcs or as args. Never split a sentence into fragment keys.
- [ ] **Step 3: Scan test → translations ×4** (expect ~55 keys; match tone; keep `[key]` tokens/glyphs).
- [ ] **Step 4: Run** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS (tests assert English; if one asserts a restructured sentence, the English output must be byte-identical — fix your key, not the test).
- [ ] **Step 5: Commit**

```bash
git add internal/tui/model.go internal/i18n/lang/
git commit -m "feat(tui): translate model.go status messages (wave 1 of 3)"
```

---

### Task 8: statusMsg wave 2 — seven files

**Files:**
- Modify: `internal/tui/commit_scope.go` (7), `internal/tui/shell_escape.go` (6), `internal/tui/bookmark_popup.go` (6), `internal/tui/commit_generate.go` (5), `internal/tui/pick_commit.go` (4), `internal/tui/conflict_process.go` (4), `internal/tui/branch_compare.go` (4) — 36 sites
- Modify: all four bundles (implementer authors translations)

**Interfaces:** consumes existing display funcs; produces nothing.

Same steps and rules as Task 7 (enumerate with the same grep per file; wrap; scan test; translations ×4; full suite). Also wrap any OTHER user-facing raw literal you touch in the same statement (e.g. a prompt string beside a statusMsg) — but do not sweep unrelated parts of these files.

- [ ] **Step 1–4** as Task 7.
- [ ] **Step 5: Commit**

```bash
git add internal/tui/commit_scope.go internal/tui/shell_escape.go internal/tui/bookmark_popup.go internal/tui/commit_generate.go internal/tui/pick_commit.go internal/tui/conflict_process.go internal/tui/branch_compare.go internal/i18n/lang/
git commit -m "feat(tui): translate status messages wave 2 (commit scope, shell, bookmarks, generate, pick, conflict, compare)"
```

---

### Task 9: statusMsg wave 3 — the tail

**Files:**
- Modify (29 sites): `internal/tui/shelf_popup.go` (3), `internal/tui/review.go` (3), `internal/tui/related_prompt_popup.go` (3), `internal/tui/refresh.go` (3), `internal/tui/irebase_view.go` (2), and one site each in `worktree_popup.go`, `stash_view.go`, `stash_popup.go`, `shelf_actions.go`, `run.go`, `reword_popup.go`, `repo_popup.go`, `prefix_picker.go`, `op.go`, `mark.go`, `files_view.go`, `conflict_picker.go`, `commit_space.go`, `commit_eager.go`, `bookmark.go`
- EXCLUDED (already covered by Tasks 3–6): `identity_popup.go`, `gitconfig_popup.go`, `notice_popup.go`, `repoconfig_popup.go`, `hook_editor.go`
- Modify: all four bundles (implementer authors translations)

Same steps and rules as Task 7.

- [ ] **Step 1–4** as Task 7.
- [ ] **Step 5: Commit**

```bash
git add internal/tui/ internal/i18n/lang/
git commit -m "feat(tui): translate status messages wave 3 (remaining tail)"
```

---

### Task 10: Carried polish — FileUILanguage, picker tests, suffix decorator

**Files:**
- Modify: `internal/config/config.go` (`FileUILanguage` → delegate to `decodeFile`)
- Modify: `internal/tui/settings_tools.go` (`toolConfiguredSuffixDecorator` + new `dimSpanRunes`), `internal/tui/settings_popup.go:814` (call site)
- Test: `internal/tui/language_popup_test.go` (create if absent), `internal/tui/settings_tools_test.go` (append)

**Interfaces:** consumes `config.FileUILanguage` (unchanged signature), `dimRowStyle` (existing). Produces `dimSpanRunes(r []rune, hscroll, startW, width int) (int, int)` — internal helper.

- [ ] **Step 1: Delegate `FileUILanguage`.** Replace its body (keep signature + doc comment):

```go
func FileUILanguage(path string) string {
	if path == "" {
		return ""
	}
	c, ok, err := decodeFile(path)
	if err != nil || !ok {
		return ""
	}
	return c.UI.Language
}
```

(Check `decodeFile`'s exact signature at `internal/config/config.go` ~line 188 first; adapt the ok/err handling to what it actually returns, preserving "any problem → empty string". Existing `TestFileUILanguage` must keep passing unchanged.)

- [ ] **Step 2: Failing decorator test** (append; note `dimSpanRunes` does not exist yet):

```go
func TestDimSpanRunesCJKColumns(t *testing.T) {
	// "工具" = 2 runes, 4 display columns; suffix span in COLUMNS.
	r := []rune("工具 (configured)")
	// span starts at column 4 (after 工具), width 13 = len(" (configured)")
	from, to := dimSpanRunes(r, 0, 4, 13)
	if from != 2 || to != len(r) {
		t.Fatalf("unscrolled: got [%d,%d), want [2,%d)", from, to, len(r))
	}
	// scrolled 2 columns: the first CJK rune is cut; visible starts at 工's end
	vis := r[1:]
	from, to = dimSpanRunes(vis, 2, 4, 13)
	if from != 1 || to != len(vis) {
		t.Fatalf("scrolled: got [%d,%d), want [1,%d)", from, to, len(vis))
	}
}
```

Run: `go test ./internal/tui/ -run TestDimSpanRunes -count=1` — Expected: FAIL (undefined).

- [ ] **Step 3: Implement.** In `settings_tools.go`, add the pure helper and rewrite the decorator to use display columns throughout:

```go
// dimSpanRunes returns the rune index range [from,to) of r whose display
// columns fall inside [startW, startW+width), given the row is already
// scrolled hscroll display columns. Columns, not rune indexes — a wide
// (CJK) rune advances the column by 2.
func dimSpanRunes(r []rune, hscroll, startW, width int) (int, int) {
	from, to := -1, -1
	col := hscroll
	for i, c := range r {
		w := lipgloss.Width(string(c))
		if col >= startW && col < startW+width {
			if from < 0 {
				from = i
			}
			to = i + 1
		}
		col += w
	}
	if from < 0 {
		return 0, 0
	}
	return from, to
}

func toolConfiguredSuffixDecorator(baseW, suffixW int) rowDecorator {
	return func(visible string, hscroll, visualLine int) string {
		if visualLine != 0 || suffixW <= 0 {
			return visible
		}
		r := []rune(visible)
		from, to := dimSpanRunes(r, hscroll, baseW, suffixW)
		if from == to {
			return visible
		}
		return string(r[:from]) + dimRowStyle.Render(string(r[from:to])) + string(r[to:])
	}
}
```

Update the decorator's doc comment: spans are display COLUMNS now. Change the call site `settings_popup.go:814` from `toolConfiguredSuffixDecorator(len([]rune(base)), len([]rune(suffix)))` to `toolConfiguredSuffixDecorator(lipgloss.Width(base), lipgloss.Width(suffix))`.

- [ ] **Step 4: Picker tests** (`language_popup_test.go`; follow the package's existing popup-test style — grep `newTestModel`/model-construction helpers in nearby `_test.go` files and reuse):

```go
func TestLanguagePickerRepoOverrideHint(t *testing.T) {
	dir := t.TempDir()
	p := filepath.Join(dir, ".gg.toml")
	if err := os.WriteFile(p, []byte("[ui]\nlanguage = \"ja\"\n"), 0o644); err != nil {
		t.Fatal(err)
	}
	m := Model{repoConfigPath: p}
	m2, _ := m.openLanguagePicker()
	pop, ok := m2.(Model).topLayer().(*languagePickerPopup)
	if !ok {
		t.Fatal("picker not on top")
	}
	if !pop.repoOverride {
		t.Fatal("repoOverride must be set when the repo config sets [ui] language")
	}
	if out := pop.render(m2.(Model), ""); !strings.Contains(out, "overrides this choice") {
		t.Fatalf("hint line missing from render:\n%s", out)
	}
}
```

(Adapt the Model construction/topLayer/render-call shapes to the package's actual test helpers — read one neighboring popup test first; `openLanguagePicker` returns `(Model, tea.Cmd)`, adjust the assertion accordingly. Also add a companion test that a `SetLanguage` failure path sets a `statusMsg` containing `"language failed:"` — drive `update` with `tea.KeyEnter` on a picker whose selected lang has a bogus code, or call the enter-handling branch directly; if wiring proves impractical without large fixtures, test `config.FileUILanguage`-driven `repoOverride=false` for a config WITHOUT the key instead and note the failure-path gap in your report.)

- [ ] **Step 5: Run** `go test ./internal/config/ ./internal/i18n/ ./internal/tui/ -count=1` — PASS.
- [ ] **Step 6: Commit**

```bash
git add internal/config/config.go internal/tui/settings_tools.go internal/tui/settings_tools_test.go internal/tui/settings_popup.go internal/tui/language_popup_test.go
git commit -m "fix(tui): column-correct suffix dimming; FileUILanguage delegates to decodeFile; picker hint tests"
```

---

### Task 11: Per-language QA delta pass

**Files:**
- Modify: `internal/i18n/lang/{ja,ko,zh,ru}.toml` (values only)

Run as FOUR subagents (parallel; each edits ONLY its own bundle; NO git commands — the controller commits). Scope: only keys ADDED on this branch — each reviewer derives its list with `git diff main -- internal/i18n/lang/<code>.toml | grep '^+"'` (stage 2 already QA'd the pre-existing 517 keys; do not re-litigate them except where a new key collides in terminology with an old one — then unify, preferring the established term).

Per-language brief: same hard rules as stage 2 (values only; never keys/verbs/`[tokens]`/glyphs); checks: naturalness, terminology consistency with the EXISTING bundle (zh: fetch=获取/pull=拉取 stands; ko: register + particles; ja: 競合/件 conventions; ru: count-neutral colon forms, case agreement around `%s`), punctuation conventions established in stage 2, length sanity (modal option labels render in a narrow box — prefer compact). After edits: `go test ./internal/i18n/ ./internal/tui/ -count=1` must pass. Findings report per language to the ledger dir.

- [ ] **Step 1:** Dispatch 4 reviewers; apply edits.
- [ ] **Step 2:** `go test ./internal/i18n/ ./internal/tui/ -count=1` — PASS.
- [ ] **Step 3: Commit** (controller): `polish(i18n): per-language QA delta pass over stage-3 keys`

---

### Task 12: Docs + full verification

**Files:**
- Modify: `CHANGELOG.md` (new top entry: stage 3 — decision-option labels, six popups, statusMsg tail, suffix-dimming fix)
- Modify: `CLAUDE.md` (i18n row: append `optionDisplayName` + the options-vocabulary scan test + "TUI now fully translated except engine prose"; `padCell` moved to `i18n_display.go`)
- Check: `README.md` (update only if it enumerates translated surfaces)

- [ ] **Step 1:** Write the doc updates in each file's existing style.
- [ ] **Step 2:** Run `./test.sh race` in the FOREGROUND (600000ms timeout) and wait for it inline — do NOT background it and stop. All stages must be green; put the tail in your report.
- [ ] **Step 3: Commit** — `docs: i18n stage 3 (decision options, popups, statusMsg tail)`

---

## Task order & parallelism

1 → 2 (test needs the vocabulary). 3–9 are mutually independent but run SEQUENTIALLY (shared bundle files — parallel edits would collide). 10 independent. 11 after 1–10. 12 last. Estimated new keys: ~40 (T1) + ~120 (T3–6) + ~120 (T7–9) ≈ 280 ×4 bundles.
